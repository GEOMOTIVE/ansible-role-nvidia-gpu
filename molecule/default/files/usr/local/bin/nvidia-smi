#!/bin/bash
set -euo pipefail

if [[ "${1:-}" == "--query-gpu=name" ]]; then
  echo "Mock GPU"
  exit 0
fi

if [[ "${1:-}" == "-L" ]]; then
  echo "GPU 0: Mock GPU (UUID: GPU-mock-0000)"
  exit 0
fi

exit 0
